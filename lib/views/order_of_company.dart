import 'package:flutter/material.dart';

class OrderOfCompany extends StatelessWidget {
  const OrderOfCompany({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      
    );
  }
}